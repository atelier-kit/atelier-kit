# Atelier-Kit (Codex) — v2

- Native `/plan` does not activate Atelier.
- Explicit `/atelier ...` or user request → `atelier new`, then `state.json` workflow.
- Single skill file per `active_skill` under `.atelier/skills/`.
