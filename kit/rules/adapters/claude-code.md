# Atelier-Kit (Claude Code) — v2

- `/plan` = native planning only; do not create Atelier epics.
- `/atelier quick|plan|deep` or explicit Atelier request → use `atelier new` and follow `.atelier/active.json` + `state.json`.
- Epic truth: `.atelier/epics/<slug>/state.json`.
- On-demand skills: only `.atelier/skills/<active_skill>.md`.
