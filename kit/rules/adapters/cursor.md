# Cursor Adapter

Read `.atelier/rules/core.md` and treat it as the always-on Atelier rule.
When Atelier is active, read `.atelier/active.json` and the active epic state before coding.
Use Cursor's native plan mode for `/plan` and reserve Atelier only for `/atelier ...` requests.
