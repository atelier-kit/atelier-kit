# Atelier-Kit (generic) — v2

Read `kit/rules/core.md` (installed as `.atelier/rules/core.md`) for the full activation rules.

Summary:

- Inactive by default (`active.json` → `active: false`).
- `/plan` = no Atelier.
- `/atelier quick|plan|deep` = create epic, follow `state.json`, load one skill at a time.
