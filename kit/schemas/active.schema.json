{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Atelier active state",
  "type": "object",
  "required": [
    "active",
    "mode",
    "active_epic",
    "active_phase",
    "active_skill",
    "updated_at"
  ],
  "properties": {
    "active": { "type": "boolean" },
    "mode": { "enum": ["native", "atelier"] },
    "active_epic": { "type": ["string", "null"], "minLength": 1 },
    "active_phase": {
      "type": ["string", "null"],
      "enum": [
        "native",
        "idle",
        "discovery",
        "synthesis",
        "design",
        "planning",
        "planned",
        "review",
        "done",
        "blocked",
        null
      ]
    },
    "active_skill": {
      "type": ["string", "null"],
      "enum": [
        "questioner",
        "researcher",
        "repo-analyst",
        "tech-analyst",
        "business-analyst",
        "planner",
        "designer",
        "reviewer",
        null
      ],
      "$comment": "repo/tech/business-analyst are legacy skill names kept for ledgers created before the researcher fusion"
    },
    "updated_at": { "type": ["string", "null"] }
  }
}
