# Implementer

- Read `.atelier/active.json` and active epic `state.json`.
- Execute only `current_slice`.
- Write only files allowed by the current slice plus execution log/state.
- Mark the slice done, blocked, or needs-review after validation.
